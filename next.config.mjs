/** @type {import('next').NextConfig} */
const nextConfig = {
  // Security: strip source maps from production build to avoid leaking source
  productionBrowserSourceMaps: false,
  
  // Security: set strict CSP headers
  async headers() {
    return [
      {
        source: '/(.*)',
        headers: [
          {
            key: 'X-Content-Type-Options',
            value: 'nosniff',
          },
          {
            key: 'X-Frame-Options',
            value: 'DENY',
          },
          {
            key: 'Referrer-Policy',
            value: 'strict-origin-when-cross-origin',
          },
        ],
      },
    ];
  },
};

export default nextConfig;
